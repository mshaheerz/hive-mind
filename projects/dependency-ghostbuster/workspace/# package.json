{
  "scripts": {
    "lint": "eslint src/**/*.ts",
    "test": "jest --watch=false",
    "dev:server": "start-server",
    "build": "tsc && node dist/server/index.js"
  },
  "devDependencies": {
    "@types/jest": "^26.0",
    "jest": "^26.9.x",
    "ts-node-dev": "latest",
    "typescript": "^4.x"
  }
}
