# Real‑time API Usage Monitor

A dashboard that visualizes live request counts, latency heatmaps, and error rates for any REST or GraphQL endpoint. Data is collected via Next.js API routes and streamed to the UI with Server‑Sent Events.